const React = require('react');

const Text = require('./text');

const Breaker = React.createClass({
  render() {
    return (
      <Text>
        ####
      </Text>);
  }
});

module.exports = Breaker;
